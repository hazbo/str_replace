#include <string.h>
#include <stdlib.h>
#include "str_replace.h"

char *str_replace(char *orig, char *rep, char *with)
{
    int orig_count = strlen(orig);
    int rep_count  = strlen(rep);
    int with_count = strlen(with);

    

    return orig;
}